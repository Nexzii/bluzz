const config = {
  appId: 'com.bluzz.app',
  appName: 'bluzz',
  webDir: 'www',
  server: {
      url: 'https://maxencestievenard.ovh', // Lien vers votre site OVH
      cleartext: false // HTTPS doit être activé
  }
};

export default config;
